<div class="hero">
  <div class="hero__content">
    <h1 class="hero-title"><%= t('welcome') %></h1>
    <p class="hero-subtitle"><%= t('home.subtitle') %></p>

    <div class="button-row" style="margin-top:1.25rem;">
      <%= link_to t('home.browse_tournaments'), tournaments_path, class: "btn btn-inverse" %>
      <%= link_to t('home.see_elo'), elo_path, class: "btn btn-inverse" %>
    </div>
  </div>
</div> 